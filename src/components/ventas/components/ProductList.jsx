// src/components/ventas/components/ProductList.jsx
import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Button, Chip } from '@mui/material';
import { AddShoppingCart, Inventory } from '@mui/icons-material';

export const ProductList = ({ productos, handleAgregarProducto, calcularPrecioBs }) => {
  return (
    <Paper elevation={1} sx={{ p: 2, height: '500px', overflow: 'auto' }}>
      {/* ... Tabla de productos ... */}
    </Paper>
  );
};