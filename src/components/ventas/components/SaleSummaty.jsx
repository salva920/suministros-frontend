// src/components/ventas/components/SaleSummary.jsx
import { Table, TableBody, TableCell, TableContainer, Divider, Box, Typography, Button } from '@mui/material';
import { ReceiptLong, CheckCircle } from '@mui/icons-material';

export const SaleSummary = ({ productosVenta, finalizarVenta, calcularPrecioBs }) => {
  return (
    <Paper elevation={1} sx={{ p: 2, height: '500px', display: 'flex', flexDirection: 'column' }}>
      {/* ... Resumen de venta ... */}
    </Paper>
  );
};