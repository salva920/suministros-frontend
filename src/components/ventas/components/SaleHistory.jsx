// src/components/ventas/components/SalesHistory.jsx
import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Button, Box, Typography, Chip } from '@mui/material';
import { History, Delete } from '@mui/icons-material';

export const SalesHistory = ({ ventas, handleGenerarFactura }) => {
  return (
    <Paper elevation={3} sx={{ p: 3, backgroundColor: '#f8f9fa' }}>
      {/* ... Historial de ventas ... */}
    </Paper>
  );
};